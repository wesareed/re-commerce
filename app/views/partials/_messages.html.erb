<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
      <%= content_tag :div, value, :id => "flash_#{key}" unless value.blank?  %>
    </ul>
  </div>
<% end %>
